# Agents

This repository is the **public skillize kit**.

Do not add `drafts/`, `docs/`, or `backlog.md` here. Design notes live in
the private umbrella that owns this checkout.

Do not put a company, host, or consumer product name in this tree.

After a behaviour change, from this checkout:

```sh
uv run pytest
```

